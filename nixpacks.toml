[phases.setup]
nixPkgs = ["php82", "nodejs_18", "npm"]

[phases.build]
cmds = [
  "composer install --no-interaction --prefer-dist",
  "npm install",
  "npm run build"
]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
